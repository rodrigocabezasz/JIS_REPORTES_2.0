# Framework vendido

Este directorio contiene **agent-service-toolkit** (upstream: [JoshuaC215/agent-service-toolkit](https://github.com/JoshuaC215/agent-service-toolkit)), incluido como código en el monorepo **JIS_REPORTES_2.0**. Se eliminó el `.git` interno para un solo repositorio en GitHub.

Para actualizar desde upstream en el futuro: comparar con el repo original o volver a clonar en una rama temporal y fusionar cambios manualmente.

Las personalizaciones JIS (agente `jis-reports`, `jis_tools.py`, etc.) viven bajo `agent-service-toolkit/src/agents/`.
